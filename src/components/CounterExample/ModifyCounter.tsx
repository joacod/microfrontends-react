import { setCartCount } from '@joacod/microfrontends-utility'

// Example, modifying a simple counter observable
export const ModifyCounter = () => {
  return <button onClick={setCartCount}>Increment Counter</button>
}
